import { css } from 'styled-components';

export default css`
  :root {
    --pd-hz-sm: 3.2rem;
    --pd-hz-md: 6.4rem;
    --pd-hz-lg: calc((100% - 1200px) / 2);
    --pd-hz-xl: calc((100% - 1400px) / 2);
  }
`;
